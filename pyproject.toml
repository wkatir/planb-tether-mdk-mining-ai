[project]
name = "mdk-mining-ai"
version = "0.1.0"
description = "AI-Driven Mining Optimization & Predictive Maintenance — Tether MDK Assignment"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
